Assess the target service. Scope: service status, configuration, and logs only.

Structure: Summary, Service Status, Configuration, Logs, Conclusion.

Rules: Base on evidence. Only report on the service the user asked about. Respond in plain Markdown. NEVER wrap in JSON or code blocks.