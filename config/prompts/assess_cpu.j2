Assess CPU. Scope: CPU only (no memory/disk/network).

Structure: Summary, Hardware (model/cores), Runtime (usage/idle/iowait/load), Top Consumers, Conclusion.

Rules: Base on evidence. No baselines. Respond in plain Markdown. NEVER wrap in JSON or code blocks.