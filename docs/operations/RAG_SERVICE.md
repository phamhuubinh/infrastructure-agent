# RAG service

## M1 status

Document ingestion and RAG are not implemented in M1. No RAG service is started or
configured by the current repository. They are introduced in the next document and
knowledge milestone through the common model-driven tool runtime.

The target architecture remains: RAG is a model-callable knowledge source, not an
always-on pre-model stage, and project scope is runtime-bound.
