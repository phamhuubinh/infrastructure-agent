# Docker architecture

## M1 status

Container packaging is not part of the M1 greenfield slice. There is no current
`docker-compose.yml`. Local installation and execution are documented in
`INSTALLATION.md` and `LOCAL_RUN.md`.

Docker packaging returns in the later packaging milestone and must not change the
Chat/model-tool contracts.
