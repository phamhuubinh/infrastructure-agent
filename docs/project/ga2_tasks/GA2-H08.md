# GA2-H08 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P2  
**DEPENDENCIES:** GA2-H06 must be DONE first.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Use the H06 runtime validation path for generated shell snippets, performing syntax validation without executing commands or changing authorization semantics.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`ConfigValidator._shell()` already performs non-executing syntax validation and warns on mutating commands; isolated tests cover valid shell, malformed quotes and mutating shell. Production generation currently does not call it.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '57,108p' src/pipeline/config_validator.py
sed -n '70,95p' tests/pipeline/test_ga2_answer_quality.py
rg -n "ConfigValidator|shell|generation" src/agent/deterministic_agent.py tests/agent/test_deterministic_agent.py
```

## EDIT SCOPE

- reuse H06 integration in `src/agent/deterministic_agent.py`
- `src/pipeline/config_validator.py` only if required
- agent-level shell generation regression

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Detect shell artifact generation, run non-executing syntax validation, surface malformed generated shell before final answer, preserve warning for mutating commands, and never turn generation into authorization/execution.

## DO NOT

- Never execute the shell snippet.
- Never use `shell=True` or a runtime mutation as validation.
- Do not refuse safe generation merely because the content describes a mutating command; Orion may generate operator-reviewed commands without claiming execution.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_ga2_answer_quality.py -k "shell"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "generation" 
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
