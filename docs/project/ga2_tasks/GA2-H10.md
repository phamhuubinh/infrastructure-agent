# GA2-H10 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P2  
**DEPENDENCIES:** D08 is DONE. Prefer reuse of GA2-H12 universal final boundary.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Apply D08 SHORT/concise semantics consistently across response strategies without rerunning tools and without removing critical warnings, refusal reasons, provenance or uncertainty.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`_answer_shape_is_short()` and `_trim_to_short()` exist, but `_trim_to_short()` is currently applied only in `_assess()`. Chat, external verification, provenance/refusal and deterministic responses may bypass concise behavior. D08 session persistence is already DONE and must be reused.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "_answer_shape_is_short|_trim_to_short|requested_answer_shape" src/agent/deterministic_agent.py src/agent/session_investigation_context.py
sed -n '870,910p' src/agent/deterministic_agent.py
sed -n '1015,1033p' src/agent/deterministic_agent.py
rg -n "SHORT|ngắn thôi|short answer" tests/qa/test_ga2_epics_cd.py tests/agent
```

## EDIT SCOPE

- shared final response boundary from H12 if available; otherwise `src/agent/deterministic_agent.py`
- D08 tests plus new table-driven concise tests

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Apply concise mode to general, live environment, external verification, provenance and refusal/fallback responses. Do not recollect/rerun tools just to shorten a previous answer. Preserve lines containing critical warning/refusal/UNKNOWN/provenance information. Keep RAW and EXPLAIN_PREVIOUS semantics distinct.

## DO NOT

- Do not create a second session answer-shape field.
- Do not trim away source provenance required by the request.
- Do not turn UNKNOWN into apparent certainty by removing caveats.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_cd.py -k "short or raw or explain_previous"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "short or refusal or external or provenance"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
