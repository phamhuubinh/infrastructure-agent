# GA2-H04 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P2  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Extend the deterministic calculator from expression syntax to a narrow safe set of common Vietnamese/English natural-language arithmetic benchmark forms, preserving obvious units.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`basic_calculator.py` safely evaluates only a strict expression grammar such as `64 - 18` or `average(20,40,60)`. `DeterministicAgent._arithmetic_response()` intercepts only when `looks_like_arithmetic()` succeeds. Natural-language examples therefore fall through to chat instead of a deterministic calculator.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
cat src/pipeline/basic_calculator.py
sed -n '601,617p' src/agent/deterministic_agent.py
cat tests/pipeline/test_basic_calculator.py
rg -n "SELF_CONTAINED_REASONING|availability|downtime|còn lại" src/pipeline/request_semantics.py
```

## EDIT SCOPE

- `src/pipeline/basic_calculator.py`
- `src/agent/deterministic_agent.py` only for formatting/units if needed
- `tests/pipeline/test_basic_calculator.py`
- optional small agent-level tests

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Support at minimum: average of 20/40/60; “64 GB total, 18 GB used, how much remains” => 46 GB; “20 + 40 + 60 then divide by 3”; 99.9% availability over a supplied period => deterministic downtime. Keep a narrow parser that translates recognized natural language into safe internal arithmetic, preserves obvious units, and fails/asks when required period/input is missing.

## DO NOT

- No `eval`, Python execution, shell, arbitrary functions, variable names, or code parsing.
- Do not guess a period for availability if absent.
- Do not strip units when they are unambiguous.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_basic_calculator.py
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "arithmetic"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
