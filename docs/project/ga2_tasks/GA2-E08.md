# GA2-E08 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Answer “what source did you just use?” from **actual prior evidence/tool receipts**, not from allowed/requested source constraints.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`ProvenanceResponder` is wired in `DeterministicAgent._provenance_response()`, but it currently derives sources from `SessionInvestigationContext.active_sources`. That field describes policy/intent, not actual executed evidence. `InvestigationRequest.evidence -> EvidencePackage.facts -> Fact.provenance` already contains actual source/tool/target/capability data that can be compacted into session metadata.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "def _provenance_response|def _remember_investigation" src/agent/deterministic_agent.py
sed -n '617,640p' src/agent/deterministic_agent.py
sed -n '1818,1848p' src/agent/deterministic_agent.py
sed -n '23,165p' src/agent/session_investigation_context.py
sed -n '44,95p' src/pipeline/provenance_responder.py
sed -n '25,110p' src/pipeline/investigation_request.py
sed -n '15,158p' src/pipeline/evidence_package.py
sed -n '123,190p' src/pipeline/provenance.py
rg -n "provenance_question|sources_from_constraints" tests/qa/test_ga2_epics_eh.py
```

## EDIT SCOPE

- `src/agent/session_investigation_context.py`
- `src/agent/deterministic_agent.py`
- `src/pipeline/provenance_responder.py`
- session serialization tests and `tests/qa/test_ga2_epics_eh.py`

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

1. Add compact serializable previous-evidence receipt metadata to session state (source, tool/capability, target, relevant fact IDs, status; timestamp if already available).
2. Populate it only from actual completed investigation/external evidence; clear/replace according to “previous response” semantics.
3. `_provenance_response()` must read these receipts first.
4. Support actual Linux/SSH/Grafana/Zabbix usage even when user supplied no source constraint.
5. Represent partial/unavailable actual-source states honestly.

## DO NOT

- Do not store chain-of-thought, full raw tool output, secrets, or large evidence payloads in session.
- Do not report a requested/allowed source as “used” unless a receipt proves it.
- Do not break D08 session serialization.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_eh.py
PYTHONPATH=. pytest -q tests/agent/test_session_investigation_context.py
PYTHONPATH=. pytest -q tests/pipeline/test_source_constraints.py -k "provenance"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
