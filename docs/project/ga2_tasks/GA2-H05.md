# GA2-H05 — Local task packet

**STATUS:** TODO  
**PRIORITY:** P2  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Implement a narrow deterministic runtime classifier for simple premise/conclusion logic: `ENTAILED`, `CONTRADICTED`, `NOT_ENOUGH_INFORMATION`.

## CURRENT SOURCE EVIDENCE / EXACT GAP

A tiny `_evaluate_simple_logic()` helper currently exists only inside `tests/qa/test_ga2_epics_eh.py`; production runtime never calls it. Self-contained “logic/suy ra” requests are currently classified GENERAL and go to the model.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '60,136p' tests/qa/test_ga2_epics_eh.py
rg -n "SELF_CONTAINED_REASONING_MARKERS|logic|suy ra" src/pipeline/request_semantics.py
sed -n '270,315p' src/pipeline/request_semantics.py
sed -n '153,275p' src/agent/deterministic_agent.py
```

## EDIT SCOPE

Preferred:
- new small `src/pipeline/logical_inference.py`
- `src/agent/deterministic_agent.py` for deterministic interception
- move/replace helper-only tests with production tests in `tests/pipeline/` and agent-level regression

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Do not build a theorem prover. Support direct entailment, direct contradiction, simple universal->named-instance benchmark form, unrelated conclusion => NOT_ENOUGH_INFORMATION, and ambiguous unsupported language => safe NOT_ENOUGH_INFORMATION/fallthrough without fabricated certainty. Parse only explicit premise/conclusion forms used by the contract.

## DO NOT

- Do not copy a test helper verbatim while leaving it disconnected.
- Do not ask the LLM to classify and call that deterministic.
- Do not infer existential/universal facts that do not logically follow.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_eh.py -k "logic"
PYTHONPATH=. pytest -q tests/pipeline -k "logical_inference"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "logic"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
