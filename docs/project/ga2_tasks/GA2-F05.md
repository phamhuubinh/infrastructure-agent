# GA2-F05 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P0  
**DEPENDENCIES:** GA2-F04 must be DONE first.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Generalize external claim grounding so concrete current version/date/price/identity/URL facts must be traceable to request-relevant extracted evidence.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`src/model/claim_validator.py:redact_ungrounded_external_claims()` currently handles narrow regex patterns (versions/dates/prices and a small office-holder case). `DeterministicAgent._respond_external_verification()` already applies assessment guards, but grounding must use the relevant evidence produced by F04 and fail UNKNOWN when the requested exact claim is absent.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '188,242p' src/model/claim_validator.py
rg -n "apply_assessment_guards|redact_ungrounded_external_claims" src/model src/agent/deterministic_agent.py
sed -n '1400,1445p' src/agent/deterministic_agent.py
rg -n "external_current_claim|ungrounded" tests/model/test_claim_validator.py
sed -n '78,130p' tests/model/test_claim_validator.py
```

## EDIT SCOPE

- `src/model/claim_validator.py`
- assessment guard wiring only if F04 relevant-evidence data is not already passed through
- `tests/model/test_claim_validator.py`
- one agent-level external grounding regression

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Ground at minimum: current software version, release date, current price/value, current office holder/identity, and a simple factual claim explicitly requested from a supplied URL. Negative cases: fetched page lacks value; old version when latest requested; ambiguous date; requested identity absent. Missing grounding => UNKNOWN/explicit inability, not a concrete claim with a citation footer.

## DO NOT

- Do not invent a value then “validate” it against a URL receipt.
- Do not require exact prose string equality when a deterministic normalized comparison is sufficient.
- Do not weaken general numeric claim guard for environment evidence.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/model/test_claim_validator.py
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "external"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
