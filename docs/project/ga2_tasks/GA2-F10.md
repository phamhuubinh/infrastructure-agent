# GA2-F10 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Complete deterministic typed handling/tests for the external failure matrix without turning any failure into sufficient evidence.

## CURRENT SOURCE EVIDENCE / EXACT GAP

The Internet tool already covers several SSRF and content states; `test_ga2_external_failure_matrix.py` currently covers unsupported MIME, empty body, 404, 500, DNS failure and oversized/truncated body. Missing matrix coverage includes timeout, odd/invalid encoding, redirect-chain behavior and explicit mixed/public-private DNS cases at the external-verification contract level.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "def _fetch_url|content_status|redirect|timeout|_resolve_public_addresses" src/tool/internet_tool.py
sed -n '279,354p' src/tool/internet_tool.py
sed -n '446,572p' src/tool/internet_tool.py
sed -n '396,485p' src/pipeline/external_verification.py
cat tests/pipeline/test_ga2_external_failure_matrix.py
rg -n "redirect|rebinding|private|timeout|encoding" tests/tool/test_internet_tool.py
```

## EDIT SCOPE

Prefer tests first:
- `tests/pipeline/test_ga2_external_failure_matrix.py`
- `tests/tool/test_internet_tool.py`
Production only if a missing typed state is demonstrated:
- `src/tool/internet_tool.py`
- `src/pipeline/external_verification.py`

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Cover unsupported MIME, timeout, DNS failure, 404, 500, empty, oversized/truncated, odd/invalid encoding, redirect chain, mixed public/private DNS, public->private redirect. Every case gets a deterministic typed failure/content state and can never accidentally yield sufficient evidence.

## DO NOT

- Do not duplicate existing SSRF implementation just to satisfy tests.
- Do not mock away the state mapping being tested.
- Do not weaken pinned-address or redirect revalidation.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_ga2_external_failure_matrix.py
PYTHONPATH=. pytest -q tests/tool/test_internet_tool.py -k "redirect or private or timeout or encoding or empty or truncated"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
