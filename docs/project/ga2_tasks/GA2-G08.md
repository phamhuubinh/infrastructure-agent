# GA2-G08 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Tighten effective SSH `PermitRootLogin` reporting for Match/context-sensitive configuration while keeping a safe fallback when effective context cannot be evaluated.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`src/tool/linux/capabilities/security.py:_get_ssh()` already prefers `sshd -T` and falls back to raw `/etc/ssh/sshd_config`. Existing tests prove effective defaults and raw fallback. Remaining gap: explicit context-aware `sshd -T -C ...` semantics and a typed distinction between global effective config, context-specific unknown, raw fallback and unavailable.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '1,108p' src/tool/linux/capabilities/security.py
rg -n "GA2-G08|sshd -T|permit_root_login" tests/tool/test_linux_tool.py
sed -n '585,700p' tests/tool/test_linux_tool.py
```

## EDIT SCOPE

- `src/tool/linux/capabilities/security.py`
- `tests/tool/test_linux_tool.py`

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

1. Keep plain `sshd -T` global effective inspection.
2. When safe connection context is actually available, support the read-only equivalent of `sshd -T -C user=...,host=...,addr=...`.
3. If Match context may matter but inputs are insufficient, report `context_specific_unknown` rather than asserting a global value applies to every context.
4. Keep raw-config fallback explicit; absent raw directive must not become `no`.
5. Output permit_root_login in the known OpenSSH values or UNKNOWN, plus source/status.

## DO NOT

- No network login attempt.
- No sshd restart/reload/mutation.
- Do not parse Match blocks as if they were globally active.
- Do not report `no` merely because a directive is absent.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/tool/test_linux_tool.py -k "sshd or permit_root"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
