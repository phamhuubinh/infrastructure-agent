# GA2-H03 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** Preserve completed H02/H04/H05 behavior; do not merge their semantics.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Make response strategy explicit across major request classes so conceptual/general/self-contained/code-generation requests do not inherit infrastructure-assessment behavior.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`AnswerStrategy` already exists in `execution_trace.py`, but GENERAL_CHAT in `run_with_steps()` currently returns `execution_trace=None`; content generation and ordinary general chat share `chat()`. Environment assessment has explicit deterministic/LLM strategies. The remaining task is to make strategy selection observable and request-appropriate without creating a second router.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '20,55p' src/pipeline/execution_trace.py
rg -n "RoutingStatus.GENERAL_CHAT|AnswerStrategy" src/agent/deterministic_agent.py
sed -n '330,470p' src/agent/deterministic_agent.py
rg -n "ExecutionIntent.GENERATE_CONTENT|RequestDomain.GENERAL" src/agent/deterministic_agent.py src/pipeline/request_semantics.py
rg -n "answer_strategy" tests/qa/test_golden_schema.py tests/qa/test_run_baseline.py tests/agent/test_deterministic_agent.py
```

## EDIT SCOPE

- `src/pipeline/execution_trace.py` only if existing enum is insufficient
- `src/agent/deterministic_agent.py`
- table-driven strategy tests in existing agent/QA tests

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Cover explicit strategies for: general explanation, translation/rewrite, self-contained reasoning, live environment facts, external verification, provenance answer, multi-source comparison, code/config generation. Reuse existing semantic frame/routing; strategy is response construction/observability, not a new intent classifier. Simple conceptual responses must not receive risk/assessment boilerplate.

## DO NOT

- Do not create another parallel routing taxonomy unless strictly required.
- Do not reroute live environment requests to general chat.
- Do not change protected safety/refusal/source/target semantics.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py
PYTHONPATH=. pytest -q tests/qa/test_golden_schema.py tests/qa/test_run_baseline.py
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
