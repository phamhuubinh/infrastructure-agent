# GA2-H11 — Local task packet

**STATUS:** TODO  
**PRIORITY:** P2  
**DEPENDENCIES:** Run last, after H03/H06/H07/H08/H10/H12 are stable.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Add deterministic response/token/latency budget controls by response strategy, then expose measurable internal metrics without dropping required safety or grounding.

## CURRENT SOURCE EVIDENCE / EXACT GAP

Global model `max_tokens` exists in runtime/config and execution traces already carry duration. There is no clear per-answer-strategy output budget, and earlier QA observed long-tail latency/oversized general responses. This task is intentionally last so it can budget the final response architecture rather than creating temporary branches.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "max_tokens" src/agent/runtime_factory.py src/model/llm_client.py src/model/providers
rg -n "total_duration_ms|runtime_metrics|AnswerStrategy" src/agent/deterministic_agent.py src/pipeline/execution_trace.py
rg -n "latency_ms|response size|token" tests/qa/test_acceptance_scoring.py tests/qa/test_unified_qa.py tests/qa/test_run_baseline.py
sed -n '1621,1665p' src/agent/deterministic_agent.py
```

## EDIT SCOPE

Keep scope narrow:
- final response/generation policy location established by H03/H12
- trace/runtime metrics
- tests for strategy budgets
Avoid global provider rewrites unless required by an existing interface.

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Define bounded policy by major response strategy (general explanation, code/config generation, assessment, external verification, deterministic/refusal). Avoid unnecessary collector/model rounds for stable general requests. Record/derive measurable median/p95-compatible duration metadata and response size/token-budget metadata for acceptance reporting. Safety/grounding checks remain mandatory regardless of budget.

## DO NOT

- Do not hard-code 386 answers.
- Do not improve latency by skipping verification, source/target checks, sanitization or validators.
- Do not globally shrink code/config output to the same budget as concise chat.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/qa/test_acceptance_scoring.py tests/qa/test_unified_qa.py
PYTHONPATH=. pytest -q tests/qa/test_run_baseline.py
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
