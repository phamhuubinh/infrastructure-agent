# GA2-H02 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Make user-supplied values authoritative end-to-end for rewrite/summarize/arithmetic/comparison/hypothetical analysis unless the user explicitly requests live collection/comparison.

## CURRENT SOURCE EVIDENCE / EXACT GAP

Semantic classification already has generation and self-contained reasoning markers, so many supplied-data requests avoid environment routing. Existing H02 tests merely inspect strings and do not prove the agent avoids collectors or preserves the supplied values in the final response.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "_GENERATION_MARKERS|_SELF_CONTAINED_REASONING_MARKERS|CONTENT_GENERATION|GENERAL" src/pipeline/request_semantics.py
sed -n '230,315p' src/pipeline/request_semantics.py
sed -n '400,530p' src/pipeline/request_semantics.py
rg -n "RoutingStatus.GENERAL_CHAT|def chat|def _arithmetic_response" src/agent/deterministic_agent.py
sed -n '153,275p' src/agent/deterministic_agent.py
sed -n '601,617p' src/agent/deterministic_agent.py
sed -n '136,170p' tests/qa/test_ga2_epics_eh.py
rg -n "content_generation_skips|conceptual_technical" tests/agent/test_deterministic_agent.py
```

## EDIT SCOPE

- `src/pipeline/request_semantics.py` only if routing gaps are demonstrated
- `src/agent/deterministic_agent.py` only if an agent path overwrites supplied data
- `tests/qa/test_ga2_epics_eh.py`
- `tests/agent/test_deterministic_agent.py`

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Add agent-level tests proving the execution engine is not called and supplied values remain in the response/prompt path for: rewrite of “CPU ổn, RAM ổn, disk 92%”; supplied arithmetic; comparison of two supplied values; hypothetical config analysis. Add a contrasting test where user explicitly asks to compare supplied value with live target and collection is allowed.

## DO NOT

- Do not solve this by classifying every infra noun as GENERAL.
- Do not remove explicit live-check semantics.
- Do not insert localhost data into self-contained tasks.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_eh.py -k "supplied"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "generation or conceptual or supplied"
PYTHONPATH=. pytest -q tests/pipeline/test_request_semantics.py
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
