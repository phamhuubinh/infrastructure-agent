# GA2-H12 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P2  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Move repetition/degeneration protection to a universal final user-visible output boundary so every response strategy is covered.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`RepetitionDetector.detect()` is implemented and unit-tested. In production it is currently called in `_assess()` after assessment sanitation/SHORT trimming, so general chat, external verification, provenance, clarification/refusal, arithmetic and some generated-content paths can bypass it.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
cat src/pipeline/repetition_detector.py
rg -n "RepetitionDetector.detect|return .*response|def chat|_respond_external_verification|_provenance_response|_arithmetic_response" src/agent/deterministic_agent.py
sed -n '840,890p' src/agent/deterministic_agent.py
sed -n '1392,1468p' src/agent/deterministic_agent.py
sed -n '1621,1665p' src/agent/deterministic_agent.py
cat tests/pipeline/test_ga2_answer_quality.py
```

## EDIT SCOPE

- `src/agent/deterministic_agent.py`
- optional small common finalizer module only if it removes duplication cleanly
- `tests/pipeline/test_ga2_answer_quality.py`
- agent/API-boundary tests

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Create/reuse one finalization boundary for user-visible strings. Repetition recovery must preserve a useful safe prefix when possible and truncate/reject looping tails. Apply to general chat, external verification, assessment, refusal/fallback, provenance and generated content. Preserve existing hidden-reasoning/language sanitation order and never recover hidden content.

## DO NOT

- Do not simply sprinkle repeated detector calls across many branches if one shared finalizer is feasible.
- Do not truncate normal code/config merely for repeated syntax that is structurally necessary; keep detector criteria pathological.
- Do not expose pre-sanitized model text during recovery.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_ga2_answer_quality.py -k "repeat or looping or pathological"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
