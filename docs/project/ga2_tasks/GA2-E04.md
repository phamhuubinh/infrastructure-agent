# GA2-E04 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** None


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Complete a named multi-source comparison so every requested source remains independent end-to-end, including partial/unavailable outcomes and per-side provenance.

## CURRENT SOURCE EVIDENCE / EXACT GAP

Parsing already preserves a bounded allow-set for named comparisons. `ExecutionEngine` disables early completion for multiple allowed sources; `_expand_multi_source_graph()` and `CapabilityRouter.resolve_all_with_metadata()` can pin each source; `EvidenceMerge` preserves source receipts. Existing tests prove these pieces, but the full response contract still needs explicit COMPLETE/PARTIAL/UNAVAILABLE behavior without source substitution.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "def _source_constraints|multi-source allow-set" src/pipeline/request_semantics.py
sed -n '581,700p' src/pipeline/request_semantics.py
rg -n "allowed_sources|_expand_multi_source_graph|_validate_source_capability_coverage" src/pipeline/execution_engine.py
sed -n '250,315p' src/pipeline/execution_engine.py
sed -n '389,482p' src/pipeline/execution_engine.py
sed -n '127,150p' src/pipeline/capability_router.py
sed -n '46,150p' src/pipeline/evidence_merge.py
rg -n "multi_source|comparison_receipts|bounded_allow_set" tests/pipeline/test_source_constraints.py tests/pipeline/test_request_semantics.py
```

## EDIT SCOPE

Primary:
- `src/pipeline/execution_engine.py`
- `src/pipeline/evidence_merge.py` if status aggregation requires it
- `src/agent/deterministic_agent.py` or deterministic response layer only for final comparison status rendering
- `tests/pipeline/test_source_constraints.py`
- add one agent-level comparison regression in an existing GA2 test file

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

For “Grafana and Zabbix”:
1. execute both requested sources when capabilities exist;
2. never silently use Linux/SSH as a replacement;
3. keep each fact/evidence receipt tied to its actual source;
4. derive COMPLETE when both requested sides have usable evidence, PARTIAL when only one does, UNAVAILABLE when neither does;
5. final response names the missing source in PARTIAL/UNAVAILABLE cases.

## DO NOT

- Do not collapse the allow-set to `ANY`.
- Do not create an unrelated second source-state store.
- Do not satisfy comparison merely because two facts share an evidence name.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_request_semantics.py -k "multi_source"
PYTHONPATH=. pytest -q tests/pipeline/test_source_constraints.py
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_eh.py
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
