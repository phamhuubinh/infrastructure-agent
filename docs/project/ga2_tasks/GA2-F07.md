# GA2-F07 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P1  
**DEPENDENCIES:** GA2-C07 must be DONE first.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Make provider-unavailable behavior fail closed on every external route, especially the C07 compound EXTERNAL -> GENERATE path.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`DeterministicAgent._external_verification_unavailable_response()` already gives an honest deterministic response for simple external requests. The remaining risk is a compound/multi-intent route bypassing that response and falling through to stale model knowledge. This task should be small after C07 is complete.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
rg -n "_external_verification_unavailable_response|RoutingStatus.EXTERNAL_VERIFICATION|_external_verifier.collect" src/agent/deterministic_agent.py
sed -n '350,435p' src/agent/deterministic_agent.py
sed -n '1390,1475p' src/agent/deterministic_agent.py
rg -n "EXTERNAL|GENERATE|depends_on" src/pipeline/multi_intent_planner.py
rg -n "unavailable|current_external_question|provider" tests/agent/test_deterministic_agent.py tests/qa/test_ga2_epics_cd.py tests/pipeline/test_external_verification.py
```

## EDIT SCOPE

- `src/agent/deterministic_agent.py`
- `tests/agent/test_deterministic_agent.py`
- `tests/qa/test_ga2_epics_cd.py`

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Cover the same deterministic unavailable contract for: simple current query, news/weather/current value, external step inside compound generation, and supported multi-intent workflow. On unavailable evidence, no downstream generation may contain a concrete “current” value unless it came from supplied verified evidence.

## DO NOT

- No model-memory fallback.
- No generic “best effort” concrete version/date/price.
- Do not weaken simple-request unavailable wording already covered by tests.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "external or unavailable"
PYTHONPATH=. pytest -q tests/qa/test_ga2_epics_cd.py -k "external or compound"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
