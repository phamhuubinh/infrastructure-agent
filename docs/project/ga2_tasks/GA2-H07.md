# GA2-H07 — Local task packet

**STATUS:** PARTIAL  
**PRIORITY:** P2  
**DEPENDENCIES:** GA2-H06 must be DONE first.


## LOCAL-AGENT CONTRACT

- One fresh session for this task only.
- Do not read the whole backlog again.
- Do not read whole source files >300 lines; use the commands below.
- Do not invent symbols, APIs, paths, test results, or runtime behavior.
- Definition != call site; test evidence is limited to explicit assertions.
- No `qa-smoke`, `qa-full`, 386 run, Docker benchmark, or unrelated refactor.
- Before DONE: targeted tests + changed-file checks + `git diff --check` + final diff review.
- STOP after this task.

## GOAL

Use the H06 runtime validation path for generated YAML/GitHub Actions workflows and prove structural failures are caught before final answer.

## CURRENT SOURCE EVIDENCE / EXACT GAP

`ConfigValidator._github_actions()` already checks YAML parsing plus several workflow structures; isolated tests cover invalid schedule syntax, nonexistent step output and invalid YAML. The missing contract is runtime integration on generated workflow output.

## READ — TERMINAL ONLY, IN THIS ORDER

Current line ranges are hints for this snapshot. Run `rg` first; if lines moved, read the symbol, not the old number.

```bash
sed -n '57,185p' src/pipeline/config_validator.py
cat tests/pipeline/test_ga2_answer_quality.py
rg -n "ConfigValidator|github_actions|workflow" src/agent/deterministic_agent.py tests/agent/test_deterministic_agent.py
```

## EDIT SCOPE

Prefer no new architecture:
- H06 integration location in `src/agent/deterministic_agent.py`
- `src/pipeline/config_validator.py` only for a demonstrably missing structural check
- agent-level generation tests

Do not edit outside this list unless a concrete import/call/reference proves it is required; explain that evidence before editing the extra file.

## REQUIRED IMPLEMENTATION

Generated workflow tests must catch invalid YAML, invalid GitHub Actions schedule syntax, sequential `setup-python` steps pretending to be a matrix, nonexistent step outputs, and duplicate/contradictory jobs where deterministically detectable. Prove validator is called on real generated output.

## DO NOT

- Do not create a second validator pipeline.
- Do not call GitHub/network.
- Do not execute workflow content.

## TARGETED ACCEPTANCE TESTS

```bash
PYTHONPATH=. pytest -q tests/pipeline/test_ga2_answer_quality.py -k "github or yaml or schedule or output"
PYTHONPATH=. pytest -q tests/agent/test_deterministic_agent.py -k "workflow or github"
git diff --check
```

Add/extend deterministic tests for the cases above in the listed test files. Do not claim DONE from helper-only tests if the contract requires agent/runtime integration.

## DONE WHEN

- Required behavior is exercised through the real path named above.
- Negative/fail-closed cases pass.
- Existing targeted regressions pass.
- `git diff --check` passes and final diff contains no unrelated edits.

## IMPLEMENTATION EVIDENCE

Not implemented yet.
