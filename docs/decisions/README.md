# Architecture decisions

These ADRs define the clean-sheet Orion architecture. Add a new ADR to change an accepted architectural decision; do not silently change architecture in implementation code.
