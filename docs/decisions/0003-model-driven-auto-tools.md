# ADR 0003 — Model-driven automatic tool use

## Decision

The model decides semantically whether and which registered tool to call.

The user does not orchestrate tool selection. Orion validates and executes model tool calls.

## Rejected

Application-side keyword/tool routing before the model.
