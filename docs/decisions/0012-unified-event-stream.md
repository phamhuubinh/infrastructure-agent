# ADR-0012: One canonical event stream

Status: Accepted

## Decision

UI progress, audit logs, and metrics project from one typed redacted lifecycle event stream. Do not maintain separate counters that reinterpret runtime success independently.
