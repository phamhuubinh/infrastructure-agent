[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomous-agent"
version = "0.1.0"
description = "A project for building autonomous agents."
authors = [{name = "Your Name", email = "you@example.com"}]
dependencies = []
