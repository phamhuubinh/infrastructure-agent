# Changelog

## Unreleased — Greenfield rebuild

The repository is being rebuilt around a model-native tool loop, canonical capability registry, harness-owned authority, explicit permission/approval policy, bounded execution, and evidence-by-reference.

The rebuild intentionally does not preserve obsolete runtime protocol semantics unless a documented external compatibility requirement exists.
